
public class Main {
    public static void main(String[] args) {
        Parent parent = new Parent();
        Child parents = new Child();

        //Child child = new Child();

        // The output is based on the reference type, not the object type
        //parent.show(); // Output: Parent's static method
       // parents.show(); // Output: Parent's static method
       // child.show();  
    }
}

